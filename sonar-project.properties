sonar.host.url=https://sonarcloud.io
sonar.organization=sennar19
sonar.projectKey=Sennar19_new_k8s_kind_from_0

# These are name and version displayed in the SonarCloud UI
sonar.projectName=new_k8s_kind_from_0
sonar.projectVersion=1.0.0 # TODO: to read from a "version.txt" file

# Path is relative to the sonar-project.properties file (replace "\" by "/", on Windows)
sonar.sources=.

# Encoding of the source code (default is default system encoding)
sonar.sourceEncoding=UTF-8