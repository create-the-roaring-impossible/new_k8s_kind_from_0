name: 'Terraform CI\CD'
on:
  workflow_dispatch:
jobs:
  terraform:
    name: Terraform
    runs-on: 'arc-desktop-s8glse7'
    container:
      image: ghcr.io/create-the-roaring-impossible/tf:latest
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.DOCKER_TOKEN }}
      options: --user root
    steps:
      - name: TEST
        run: |
          ls -larth /__w/
          sudo chmod 777 /__w/
          sudo chown root:root /__w/
          ls -larth /__w/
      - name: 'Repository Checkout'
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Terraform Init
        run: |
          terraform --version