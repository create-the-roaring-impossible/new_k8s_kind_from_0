---
apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
# Cluster purpose: Local development environment with registry integration
# VERSION: 1.0.0
# LAST UPDATED: 2025-09-22
networking:
  # WARNING: It is _strongly_ recommended that you keep this the default (127.0.0.1) for security reasons.
  apiServerAddress: "127.0.0.1"
  # By default the API server listens on a random open port.
  # Using a random port makes it easier to spin up multiple clusters.
  apiServerPort: 10443
  # The default CNI will not be installed
  disableDefaultCNI: true
  # Calico CNI Installation:
  # After cluster creation, run: make install_calico_cni
  # This will install Calico CNI using Helm with custom values from config/calico-values.yaml
  # See https://projectcalico.docs.tigera.io/getting-started/kubernetes/kind/ for details
containerdConfigPatches:
  - |-
    [plugins."io.containerd.grpc.v1.cri".registry.mirrors."localhost:5000"]
      endpoint = ["http://local-registry:5000"]
featureGates:
  # Enable specific Kubernetes features if needed
  EphemeralContainers: true
# # # Node Health Monitoring:
# # # kubeletConfigPatches:
# # #   - |
# # #     apiVersion: kubelet.config.k8s.io/v1beta1
# # #     kind: KubeletConfiguration
# # #     evictionHard:
# # #       memory.available: "200Mi"
# # #       nodefs.available: "10%"
# # #     healthzPort: 10248
# # #     healthzBindAddress: "0.0.0.0"
# # #     systemReserved:
# # #       cpu: "200m"
# # #       memory: "500Mi"
# # #     kubeReserved:
# # #       cpu: "200m"
# # #       memory: "500Mi"
nodes:
  - role: control-plane
    # # # name: control-plane-1
    # kubeadmConfigPatches:
    #   - |
    #     kind: InitConfiguration
    #     nodeRegistration:
    #       kubeletExtraArgs:
    #         node-labels: "role=control-plane,ingress-ready=true,node=control-plane-1"
    #         register-taints: "node-role.kubernetes.io/control-plane:NoSchedule"
    # extraMounts:
    #   - hostPath: /var/run/docker.sock
    #     containerPath: /var/run/docker.sock
    # # # resources:
    # # #   limits:
    # # #     cpu: "2"
    # # #     memory: "4Gi"
    extraPortMappings:
      - containerPort: 80
        hostPort: 80
        protocol: TCP
      - containerPort: 443
        hostPort: 443
        protocol: TCP
  - role: worker
    # # # name: worker-1
    # kubeadmConfigPatches:
    #   - |
    #     kind: JoinConfiguration
    #     nodeRegistration:
    #       kubeletExtraArgs:
    #         node-labels: "role=worker,ingress-ready=true,node=worker-1,workload=general"
    # extraMounts:
    #   - hostPath: /var/run/docker.sock
    #     containerPath: /var/run/docker.sock
    # # # resources:
    # # #   limits:
    # # #     cpu: "2"
    # # #     memory: "4Gi"
  - role: worker
    # # # name: worker-2
    # kubeadmConfigPatches:
    #   - |
    #     kind: JoinConfiguration
    #     nodeRegistration:
    #       kubeletExtraArgs:
    #         node-labels: "role=worker,ingress-ready=true,node=worker-2,workload=general"
    # extraMounts:
    #   - hostPath: /var/run/docker.sock
    #     containerPath: /var/run/docker.sock
    # # # resources:
    # # #   limits:
    # # #     cpu: "2"
    # # #     memory: "4Gi"










# # This script will run after the cluster is created
# postCreateHooks:
# - |
#   #!/bin/bash

#   # DESCRIPTION: Creates a Kind cluster and installs Calico CNI
#   # REQUIREMENTS: kind, kubectl, helm
#   # USAGE: bash install_kind_with_calico.sh
#   # EXAMPLE: bash install_kind_with_calico.sh
#   # AUTHORS: GitHub Copilot
#   # VERSION: 1.0.0
#   # DATE: 2025-09-22

#   # Validate requirements
#   command -v kind >/dev/null 2>&1 || { echo "kind is required but not installed. Aborting."; exit 1; }
#   command -v kubectl >/dev/null 2>&1 || { echo "kubectl is required but not installed. Aborting."; exit 1; }
#   command -v helm >/dev/null 2>&1 || { echo "helm is required but not installed. Aborting."; exit 1; }

#   # Create the cluster
#   make create_kind_cluster_with_registry

#   # Wait for the control plane to be ready
#   echo "Waiting for control plane to be ready..."
#   until kubectl get nodes | grep control-plane-1 | grep -q " Ready "; do
#     sleep 2
#   done

#   # Install Calico CNI
#   make install_calico_cni

#   echo "Kind cluster with Calico CNI setup complete."










# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: example-app
# spec:
#   selector:
#     matchLabels:
#       app: example
#   template:
#     metadata:
#       labels:
#         app: example
#     spec:
#       affinity:
#         nodeAffinity:
#           preferredDuringSchedulingIgnoredDuringExecution:
#           - weight: 100
#             preference:
#               matchExpressions:
#               - key: role
#                 operator: In
#                 values:
#                 - worker
#       # To override the NoSchedule taint on the control-plane:
#       tolerations:
#       - key: "node-role.kubernetes.io/control-plane"
#         operator: "Exists"
#         effect: "NoSchedule"